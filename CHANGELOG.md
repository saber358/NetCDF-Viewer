# 更新日志

本文件记录项目的主要版本变化。

## 1.0.5

- 新增波浪波向叠加，自动识别兼容的 `wdir` / `wlen` 变量对，并在平面图上绘制波向箭头
- 新增海流流场叠加，自动识别兼容的 `u` / `v` 或 `ua` / `va` 变量对，并以动态黑色短流线显示流向
- 优化海流流线观感，缩短单段笔触并增加路径平滑处理，减少转折生硬和动画抖动
- 新增波向叠加与海流流线叠加相关识别、渲染和回归测试
- 更新 GitHub / Gitee `README`，补充最近新增功能说明与波向、海流预览截图
- 版本号升级到 `1.0.5`

## 1.0.4

- 新增 GeoJSON / shapefile 海岸线叠加，可作为参考边界覆盖到当前平面图之上
- 海岸线叠加跟随当前视图缩放、平移和数据集切换一起刷新
- 新增内置默认海岸线资源，程序启动后默认启用，无需用户手动加载文件
- 新增 `Use Built-in Coastline` 菜单入口，可在清空或替换外部海岸线后恢复系统内置海岸线
- 新增本地海岸线样例数据：`samples/coastline/ne_110m_coastline.geojson` 与 `samples/coastline/ne_110m_coastline.shp`
- 新增真实海岸线样例解析回归测试，验证下载到本地的 GeoJSON 和 SHP 文件可被成功读取
- 重新打包 Windows 安装程序，更新 `NetCDFViewer-1.0.4.exe`
- 版本号升级到 `1.0.4`

## 1.0.3

- 新增多数据集管理，支持在已加载一个 `.nc` 后继续追加加载其他 `.nc` 数据
- 左侧新增已加载数据集列表，可切换当前活动数据集
- 支持删除已加载但当前不需要的数据集
- 同一路径重复打开时不再重复添加，而是自动切换到已加载的数据集
- 切换活动数据集时同步刷新变量列表、元数据面板、渲染区与单点查询上下文
- 新增多数据集管理回归测试
- 重新打包 Windows 安装程序，更新 `NetCDFViewer-1.0.3.exe`
- 版本号升级到 `1.0.3`

## 1.0.2

- 修复 PNG 导出链路的健壮性问题
- 新增导出后文件可读回校验，避免静默生成损坏图片
- 新增打包运行时导出回归测试
- 新增平面图单点查询，支持鼠标点击查询当前点对应的网格值
- 单元中心变量返回命中三角形原始值，节点中心变量返回三角形内插值结果
- 新增单点查询交互与几何计算回归测试
- 重新打包 Windows 安装程序，更新 `NetCDFViewer-1.0.2.exe`
- 版本号升级到 `1.0.2`

## 1.0.1

- 新增作者信息展示：`lwj / 2762692204@qq.com`
- 新增 `Help -> About`
- 更换应用与安装包图标
- 更新安装包元数据与版本号

## 1.0.0

- 完成 NetCDF 非结构化三角网桌面可视化基础能力
- 支持节点 / 单元属性变量平面渲染
- 支持多层变量深度切换
- 支持 PNG 导出
- 支持 Windows `.exe` 打包
